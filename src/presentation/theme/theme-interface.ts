export interface ThemeInterface {
  colors: {
    disabled: string;
    lightContrast: string;
    darkContrast: string;
    background: string;
    primary: string;
    primaryDark: string;
    primaryLight: string;
    textLight: string;
    textDark: string;
  };
  font: {
    regular: 'Poppins';
  };
}
