export interface ThemeInterface {
  colors: {
    lightContrast: string;
    darkContrast: string;
    background: string;
    primary: string;
    primaryDark: string;
    primaryLight: string;
    textLight: string;
    textDark: string;
  };
  font: {
    regular: 'Poppins';
  };
}
