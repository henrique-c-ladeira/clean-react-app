export interface ThemeInterface {
  colors: {
    background: string;
    primary: string;
    primaryDark: string;
    primaryLight: string;
    textLight: string;
    textDark: string;
  };
  font: {
    regular: 'Poppins';
  };
}
